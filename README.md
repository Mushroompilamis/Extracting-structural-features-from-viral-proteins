# Extracting-structural-features-from-viral-proteins

`Note 1:`
- The Jupyter Notebooks are configured to run in Google Colab.
- Input files are loaded from the data directory, while some generated CSV files (not from pipelines, only ChimeraX outputs and R’s) and figures are saved in the predefined Colab output folders and then manually added to the corresponding folders with the same names in the GitHub repository.

